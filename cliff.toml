#
# cliff.toml — Strict Conventional Commit (exclude non-conventional)
#

[git]
conventional_commits = true
filter_unconventional = true

[changelog]
header = """
# Changelog
"""
trim = true
footer = "\n<hr>\n"

body = """
## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}

{% for group, commits in commits | group_by(attribute="group") %}
### {{ group }}

{% for commit in commits %}
- {{ commit.message | trim }} ({{ commit.id | truncate(length=7, end="") }})
{% endfor %}

{% endfor %}
"""

[commit_types]

feat = { group = "✨ Features" }
fix = { group = "🐛 Bug Fixes" }
docs = { group = "📚 Documentation" }
test = { group = "🧪 Tests" }
refactor = { group = "🛠 Refactoring" }
ci = { group = "🤖 CI" }
chore = { group = "💼 Other" }
perf = { group = "🚀 Performance" }
build = { group = "📦 Build / Packaging" }