
site_name: c108

theme:
  name: material

  # Colors palette
  palette:
    - scheme: default
      primary: indigo
      accent: pink

    # Dark mode scheme
    - scheme: slate
      primary: indigo
      accent: pink
      toggle:
        icon: material/weather-night
        name: Switch to light mode

  # Feature toggles
  features:
    - content.code.copy
    - navigation.tabs       # top-level sections as tabs
    - navigation.sections
    - navigation.tabs
    - navigation.sections
    - navigation.tracking
    - search.suggest
    - search.highlight
    - toc.integrate

